// Re-export from the canonical location so layouts can keep importing from '../utils'.
export { toSpotifyEmbed, toSpotifyUri, parseSpotifyUrl, validateSpotifyUrl } from '@/utils/spotify';
export type { ParsedSpotifyUrl, SpotifyResourceType, SpotifyValidation } from '@/utils/spotify';
